# Shared Contracts

This folder is reserved for shared API contracts and client-facing types.

- `api.ts` contains starter TypeScript types that mirror backend concepts
- UI apps should consume backend contracts rather than touching hardware directly
